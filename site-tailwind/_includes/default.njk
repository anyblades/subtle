{% set site = pkg.site | merge(site) | attr_set('styles', []) | attr_concat('styles', pkg.site.styles) | attr_concat('styles', site.styles) %}
{% extends 'blades/html.njk' %}

{% block body %}
  <main>
    <article class="prose lg:prose-xl xl:prose-2xl breakout-all">{{ content | safe }}</article>
  </main>
  <footer class="prose">
    <hr />
    {{ site.footer_contents | join(' ') | renderContent('njk,md', site) | safe }}
  </footer>
{% endblock %}
