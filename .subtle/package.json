{
  "private": true,
  "name": "subtle.blades.ninja",
  "type": "module",
  "scripts": {
    "start": "npm run build -- --serve",
    "stage": "rm -r ./_site || true && npm run build && serve ./_site",
    "build": "eleventy --input=../",
    "dev": "npm i --install-links && rm -r ./node_modules/@anyblades/eleventy-blades && cd ./node_modules/@anyblades/ && ln -s ../../../../eleventy-blades"
  },
  "author": "Anton Staroverov",
  "license": "MIT",
  "devDependencies": {
    "prettier-plugin-jinja-template": "^2.1.0",
    "serve": "^14.2.5"
  },
  "dependencies": {
    "@anyblades/subtle": "^0.0.1-alpha"
  }
}
